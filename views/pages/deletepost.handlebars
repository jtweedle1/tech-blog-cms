<div id="post-id">{{post.id}}</div>

<div class="flex-center">

<h1>Are you sure you want to delete this post?</h1>

<div class="card col-4">
  <div class="card-body">
    <h5 class="card-title">{{post.title}}</h5>
    <p class="card-text">
      {{post.content}}
    </p>
  </div>
</div>
</div>

<div class="flex-center">
    <a href="/dashboard"><button class="btn btn-primary" type="submit" id="delete-button">Yes</button></a><br>
    <a href="/dashboard"><button class= "btn btn-primary">No</button></a>
</div>

<script src="/js/update-post.js"></script>